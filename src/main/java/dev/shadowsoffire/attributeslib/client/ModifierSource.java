package dev.shadowsoffire.attributeslib.client;

/**
 * 1.16.4 rendering wrapper used by {@link AttributesGui} to represent where an attribute modifier
 * came from (equipment slot, active effect, ...). The 1.12.2 GUI port is not yet implemented, so
 * this is stubbed out.
 */
public abstract class ModifierSource<T> {

    protected ModifierSource() {}
}
